import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sirio_appV2.0',
  appName: 'sirio_appV2.0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
